{
  "name": "desire-uniforms",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "rm -rf .next && next build",
    "start": "next start",
    "lint": "next lint",
    "export": "next export",
    "deploy": "npm run build && npm run export"
  },
  "dependencies": {
    "@next/bundle-analyzer": "^15.1.7",
    "compression-webpack-plugin": "^11.1.0",
    "next": "15.1.5",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-icons": "^5.4.0",
    "tailwindcss": "^3.4.1"
  },
  "devDependencies": {
    "postcss": "^8"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
